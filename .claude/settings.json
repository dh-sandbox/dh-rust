{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && FILE_PATH=$(cat | jq -r '.tool_input.file_path // empty') && [ -n \"$FILE_PATH\" ] && qlty fmt \"$FILE_PATH\" 2>/dev/null; qlty check \"$FILE_PATH\" 2>&1 | head -50",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
